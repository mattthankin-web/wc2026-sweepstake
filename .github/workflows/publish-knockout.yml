name: Publish Knockout Tab

# This workflow has already been superseded — the knockout tab
# was added manually. Keeping this as a no-op to avoid further failures.
on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Already published
        run: echo "Knockout tab was published manually. Nothing to do."
